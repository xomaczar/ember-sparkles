
import { eSLine } from 'dummy/helpers/e-s/line';
import { module, test } from 'qunit';

module('Unit | Helper | e s/line');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eSLine([42]);
  assert.ok(result);
});

